import { Response } from "express";
//import { GetPostsRequest } from "../interfaces";
import Post from "../models/Post";
//import { NewPostRequest } from "../interfaces/index";

//export const getPosts = async (req: GetPostsRequest, res: Response) => {};

/*
export const newPost = async (req: NewPostRequest, res: Response) => {
  const {
    title,
    content, 
    img,
    slug,
  }
}
*/